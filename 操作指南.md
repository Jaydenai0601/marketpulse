# MarketPulse — 从零到跑通完整指南

## 第一步：获取 DeepSeek API Key（免费额度足够用）

1. 打开浏览器，访问 https://platform.deepseek.com
2. 用手机号注册账号（很简单，1分钟搞定）
3. 登录后点击左侧「API Keys」
4. 点击「创建 API Key」，复制生成的字符串（形如 `sk-xxxxxxxx`）
5. **把这个 Key 保存好**，后面会用到

> DeepSeek 新用户有 10 元免费额度，分析几百条新闻都够用。

---

## 第二步：安装 Python 依赖

在终端执行以下命令：

```bash
cd /workspace/backend
pip install requests beautifulsoup4 lxml openai --break-system-packages
```

> `--break-system-packages` 是为了避免虚拟环境的问题，如果你用了虚拟环境可以去掉。

---

## 第三步：设置 API Key 并运行

### Mac / Linux：

```bash
cd /workspace/backend
export DEEPSEEK_API_KEY="sk-xxxxxxxx"   # 替换成你复制的 Key
python run.py
```

### Windows：

```powershell
cd /workspace/backend
$env:DEEPSEEK_API_KEY="sk-xxxxxxxx"    # 替换成你复制的 Key
python run.py
```

运行后你会看到：
- 📡 正在抓取财经新闻...（抓取东方财富、新浪财经）
- 🤖 开始 AI 情绪分析（共 XX 条）...
- 📊 聚合分析数据...
- 📝 生成日报...
- ✅ 完成！

整个过程大约 **2-5 分钟**（取决于新闻条数和 API 响应速度）。

---

## 第四步：查看结果

运行完成后，会在 `/workspace/data/` 目录生成两个文件：

| 文件 | 内容 |
|------|------|
| `daily_data.json` | 当日完整分析结果（情绪指数、板块热力、Top 3 事件、日报文本） |
| `history.json` | 历史数据归档 |

你可以在终端用以下命令查看：

```bash
cat /workspace/data/daily_data.json
```

或者直接用文本编辑器打开看。

---

## 第五步：前端接入真实数据

前端 `index.html` 目前用的是**模拟数据**。跑通后端后，需要改成读取 `daily_data.json`。

有两种方式：

### 方式 A：手动替换（最快，适合展示）

把 `daily_data.json` 里的内容复制到 `index.html` 的模拟数据区域，替换掉现有的假数据。

### 方式 B：自动读取（推荐，后续可持续更新）

修改 `index.html`，让它通过 fetch 读取 JSON 文件。我来帮你改好这一步。

**你要我直接帮你把前端改成自动读取 JSON 吗？** 如果是，我直接改好代码，你刷新页面就能看到真实数据。

---

## 第六步：每天自动更新（可选）

如果你想让系统每天早上自动跑一轮，可以用系统的定时任务：

### Mac / Linux（crontab）：

```bash
crontab -e
```

添加一行：

```
0 8 * * * cd /workspace/backend && export DEEPSEEK_API_KEY="sk-xxxxxxxx" && python run.py
```

这表示每天早上 8:00 自动运行。

### Windows：

用「任务计划程序」创建一个每天定时运行的任务，执行上面的命令。

---

## 常见问题

**Q: 爬虫抓不到新闻？**
- 检查网络连接
- 某些网站可能有反爬，多试几次
- 如果东方财富抓不到，新浪财经通常可以

**Q: API 调用失败？**
- 检查 API Key 是否正确（不要有多余空格）
- 检查 DeepSeek 账户余额是否充足
- 可能是网络问题，多试一次

**Q: 分析结果不准？**
- 新闻只有标题时，分析精度会下降（因为缺少正文细节）
- 可以在 `run.py` 里把 `fetch_content=False` 改成 `True`，让爬虫尝试抓取正文
- 也可以在 `analyzer.py` 里优化 Prompt，调整判断标准

---

## 你现在该做什么

1. **现在就去** https://platform.deepseek.com 注册并复制 API Key
2. 回来执行上面第二步和第三步的命令
3. 跑通后告诉我结果，我帮你把前端改成读取真实数据

有遇到任何问题随时问我。
